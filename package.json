{
  "name": "bnyad-backend-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "cd vercel-api && npm run dev",
    "build": "cd vercel-api && npm install && npm run build",
    "vercel-build": "cd vercel-api && npm install && npm run vercel-build"
  },
  "dependencies": {
    "next": "^15.3.4"
  }
}
